// Will be used when MongoDB is added

/*
const mongoose = require('mongoose');

const quoteSchema = new mongoose.Schema({
  fullName: String,
  email: String,
  phone: String,
  website: String,
  services: [String],
  notes: String
}, { timestamps: true });

module.exports = mongoose.model('Quote', quoteSchema);
*/
